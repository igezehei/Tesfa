type Agent {
    id: ID!
    name: String!
    description: String
    avatarUrl: String
    context: String
    capabilities: [String!]
}

type Query {
    queryLLM(prompt: String!, provider: String!, options: JSON): String
    agents: [Agent!]!
}

scalar JSON
